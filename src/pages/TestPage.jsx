import React from 'react';
import ReactDOM from 'react-dom';
import Reservasi from './pages/Reservasi'; // Pastikan path benar

ReactDOM.render(
  <React.StrictMode>
    <Reservasi />
  </React.StrictMode>,
  document.getElementById('root')
);
